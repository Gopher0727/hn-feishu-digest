PUSH_MODE=company
TASK_ROLE=collector
# An existing PRIVATE repository, initialized with a README; default branch is used.
DATA_REPO=YOUR_USERNAME/hn-digest-data
DATA_TOKEN=replace-with-contents-write-token
# Relative paths are relative to the project directory.
DATA_DIR=./data
STATE_DIR=./state-collector
